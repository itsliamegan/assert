assert
======

Assert two values are deeply equal (according to reflect.DeepEqual) and fail a
test if they are not.

	package main

	import (
		"testing"

		"github.com/itsliamegan/assert"
	)

	func TestExample(t *testing.T) {
		assert := assert.New(t)

		result := 2 + 2

		assert.Equal(4, result)
	}
